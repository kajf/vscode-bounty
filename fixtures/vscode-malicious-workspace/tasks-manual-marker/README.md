# Benign manual-task marker fixture

This fixture confirms the baseline: repository-defined tasks can execute commands only after deliberate user task execution. Running `benign-manual-marker` writes `manual-task-marker.txt`.
